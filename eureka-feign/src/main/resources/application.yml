spring:
  application:
    name: eureka-feign

server:
  port: 1005

## feign内置熔断 默认是关闭的
feign:
  hystrix:
    enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1001/eureka